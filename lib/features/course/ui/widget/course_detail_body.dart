import 'package:flutter/material.dart';

class CourseDetailBody extends StatelessWidget {
  const CourseDetailBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
